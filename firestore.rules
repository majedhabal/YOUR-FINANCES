rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissive access for sandbox testing and login
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
